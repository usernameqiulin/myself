'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _react = require('react');

var _react2 = _interopRequireDefault(_react);

var _reactRedux = require('react-redux');

var _enzyme = require('enzyme');

var _enzymeAdapterReact = require('enzyme-adapter-react-15.4');

var _enzymeAdapterReact2 = _interopRequireDefault(_enzymeAdapterReact);

var _jsdom = require('jsdom');

var _jsdom2 = _interopRequireDefault(_jsdom);

var _boneMv = require('@bone/bone-mv');

var _PageContainer2 = require('../PageContainer');

var _PageContainer3 = _interopRequireDefault(_PageContainer2);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

// setup file
var doc = _jsdom2.default.jsdom('<!doctype html><html><body></body></html>');
global.document = doc;
global.window = doc.defaultView;
(0, _enzyme.configure)({ adapter: new _enzymeAdapterReact2.default() });

describe('test PageComponent', function () {
  test('basic usage', function () {
    expect.assertions(22);

    // check child component

    var Child = function (_Component) {
      _inherits(Child, _Component);

      function Child() {
        _classCallCheck(this, Child);

        return _possibleConstructorReturn(this, (Child.__proto__ || Object.getPrototypeOf(Child)).apply(this, arguments));
      }

      _createClass(Child, [{
        key: 'componentWillMount',
        value: function componentWillMount() {
          // definition check for properties & method
          expect(this.model).toBeDefined();
          expect(this.actions).toBeDefined();
          expect(this.actions.add).toBeDefined();
          expect(this.getState).toBeDefined();

          // test redux methods
          expect(this.getState().value).toEqual(0);

          // add
          this.actions.add();
          expect(this.getState().value).toEqual(1);
          this.actions.add(2);
          expect(this.getState().value).toEqual(3);

          // minus
          this.actions.minus();
          expect(this.getState().value).toEqual(2);
          this.actions.minus(2);
          expect(this.getState().value).toEqual(0);
        }
      }, {
        key: 'render',
        value: function render() {
          return _react2.default.createElement(
            'div',
            null,
            'child'
          );
        }
      }]);

      return Child;
    }(_react.Component);

    // check controller itself


    var TestPage = function (_PageContainer) {
      _inherits(TestPage, _PageContainer);

      function TestPage() {
        _classCallCheck(this, TestPage);

        return _possibleConstructorReturn(this, (TestPage.__proto__ || Object.getPrototypeOf(TestPage)).apply(this, arguments));
      }

      _createClass(TestPage, [{
        key: 'componentWillMount',
        value: function componentWillMount() {
          // definition check for properties & method
          expect(this.model).toBeDefined();
          expect(this.actions).toBeDefined();
          expect(this.actions.add).toBeDefined();
          expect(this.getState).toBeDefined();

          // test redux methods
          expect(this.getState().value).toEqual(0);

          // add
          this.actions.add();
          expect(this.getState().value).toEqual(1);
          this.actions.add(2);
          expect(this.getState().value).toEqual(3);

          // minus
          this.actions.minus();
          expect(this.getState().value).toEqual(2);
          this.actions.minus(2);
          expect(this.getState().value).toEqual(0);
        }
      }, {
        key: 'componentDidMount',
        value: function componentDidMount() {
          // page methods
          expect(this.getNavParams()).toEqual({});
          expect(this.navParams).toEqual({});
          expect(this.history).toBeDefined();
          expect(this.navbar).toBeDefined();
        }
      }, {
        key: 'render',
        value: function render() {
          return _react2.default.createElement(Child, null);
        }
      }]);

      return TestPage;
    }(_PageContainer3.default);

    var TestModel = function (_Model) {
      _inherits(TestModel, _Model);

      function TestModel() {
        _classCallCheck(this, TestModel);

        return _possibleConstructorReturn(this, (TestModel.__proto__ || Object.getPrototypeOf(TestModel)).apply(this, arguments));
      }

      _createClass(TestModel, [{
        key: 'add',
        value: function add(params) {
          return {
            value: this.state.value + (params || 1)
          };
        }
      }, {
        key: 'minus',
        value: function minus(params) {
          return {
            value: this.state.value - (params || 1)
          };
        }
      }]);

      return TestModel;
    }(_boneMv.Model);

    TestModel.initialState = {
      value: 0
    };


    var ConnectedTestPage = (0, _boneMv.connect)(TestPage, TestModel);

    var props = {
      match: {},

      location: {},

      route: {
        initialProps: {
          value: 0
        },

        page: TestPage
      },

      // fake props.history
      history: {},

      // fake props.navbar
      navbar: {}
    };

    // should not createStore until all controller ready
    var store = (0, _boneMv.createStore)(function (state) {
      return state;
    });

    (0, _enzyme.mount)(_react2.default.createElement(
      _reactRedux.Provider,
      { store: store },
      _react2.default.createElement(ConnectedTestPage, props)
    ));
  });
});