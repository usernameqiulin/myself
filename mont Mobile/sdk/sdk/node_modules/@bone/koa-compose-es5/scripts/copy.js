#!/usr/local/bin/node
var fs = require('fs');
var path = require('path');

var src = path.resolve(__dirname + '/../node_modules/koa-compose/index.js');
var dest = path.resolve(__dirname + '/../src/index.js');

fs.copyFile(src, dest, function(err) {
  if(err) throw err;
});
